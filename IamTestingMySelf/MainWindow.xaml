<Window x:Class="IamTestingMySelf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IamTestingMySelf"
        mc:Ignorable="d"
        Title="Testiiing..." Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="Result" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
        <TextBox x:Name="procedureExpression" Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <TextBox x:Name="functionExpression" Grid.Row="3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    </Grid>
</Window>
